<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.demo.nl_questionnaire_db_choice_single.mapper.NlQuestionnaireDbChoiceSingleMapper">

    <insert id="add">
        insert into nl_questionnaire_db_choice_single(question_id, choice_content, choice_score_weight, sort, del_flag)
        values (#{questionId},
                #{choiceContent},
                #{choiceScoreWeight},
                #{sort},
                #{delFlag})
    </insert>
    <delete id="deleteByQuestionId">
        delete
        from nl_questionnaire_db_choice_single
        where question_id = #{questionId}
    </delete>
    <select id="getChoiceByQuestionId"
            resultType="org.jeecg.modules.demo.nl_questionnaire_db_choice_single.entity.NlQuestionnaireDbChoiceSingle">
        select id, question_id, choice_content, choice_score_weight, sort, del_flag
        from nl_questionnaire_db_choice_single
        where question_id = #{questionId}
    </select>
</mapper>